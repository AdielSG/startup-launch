# ── REQUIRED — the server will not start without these ────────────────────────

# Apify API token — used for ALL scraping:
#   · Twitter/X launch tweet search (apidojo/tweet-scraper)
#   · LinkedIn post engagement metrics (actor Wpp1BZ6yGWjySadk3)
# Get from: https://console.apify.com/account/integrations
APIFY_API_TOKEN=

# OpenAI API key — used to generate personalised outreach DMs (gpt-4o-mini)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=


# ── Infrastructure (defaults work for local dev) ──────────────────────────────

# Database URL — SQLite for local dev, PostgreSQL for production
# Local:      DATABASE_URL=sqlite:///./launches.db
# Production: DATABASE_URL=postgresql://user:pass@host:5432/dbname
DATABASE_URL=sqlite:///./launches.db

# Frontend API base URL — override when deploying outside localhost
# When running locally, the Vite proxy handles /api → :8000 (no change needed)
VITE_API_URL=http://localhost:8000
